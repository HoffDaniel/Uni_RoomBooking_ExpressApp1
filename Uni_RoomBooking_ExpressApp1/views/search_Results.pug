extends layout

block content
    div.row
        table(class= 'w3-table-all w3-centered')
            thead
                tr
                 th Name
            tbody
                each room in rooms
                    tr
                     td
                            |#{room.room}
                    //    a(href='/roomDetails/' + room.Name) #{room.Name}